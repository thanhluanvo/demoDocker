apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/thanhluanvo/demodocker:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/thanhluanvo/demodocker:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: demodocker
        chartPath: charts/demodocker
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/thanhluanvo/demodocker"
          image.tag: "{{.DIGEST_HEX}}"
